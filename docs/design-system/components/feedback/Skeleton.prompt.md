Placeholder pemuatan berbentuk konten aslinya.

```jsx
<SkeletonCard media lines={2} />
<SkeletonTable rows={5} cols={4} />
<Skeleton width={140} height={24} />
```

Aturan: jangan menampilkan skeleton untuk nominal saldo — tampilkan saldo terakhir yang diketahui atau tanda "—", supaya pengguna tidak melihat angka berkedip di area uang.
